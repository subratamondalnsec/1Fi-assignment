import { formatInterestRate } from '../../utils/emiHelpers';
import { formatCurrency } from '../../utils/productHelpers';

export function EmiCard({ plan, isRecommended, isSelected, onSelect }) {
  return <button aria-pressed={isSelected} className={`relative w-full rounded-xl border p-4 text-left transition focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600 ${isSelected ? 'border-indigo-600 bg-indigo-50 ring-1 ring-indigo-600' : 'border-slate-200 bg-white hover:border-indigo-300 hover:shadow-sm'}`} onClick={() => onSelect(plan)} type="button"><div className="flex items-start justify-between gap-3"><div><p className="text-lg font-bold text-slate-950">{formatCurrency(plan.monthlyAmount)} <span className="text-sm font-medium text-slate-600">/ month</span></p><p className="mt-1 text-sm font-medium text-slate-700">{plan.tenure} months · {formatInterestRate(plan.interestRate)}</p></div>{isSelected && <span className="rounded-full bg-indigo-600 px-2 py-1 text-xs font-semibold text-white">Selected</span>}</div>{isRecommended && <span className="mt-3 inline-flex rounded-full bg-emerald-100 px-2.5 py-1 text-xs font-semibold text-emerald-800">Recommended: lowest total payable</span>}{Number.isFinite(plan.cashback) && plan.cashback > 0 && <p className="mt-3 text-sm font-semibold text-emerald-700">{formatCurrency(plan.cashback)} cashback</p>}{plan.description && <p className="mt-2 text-sm leading-6 text-slate-600">{plan.description}</p>}</button>;
}
