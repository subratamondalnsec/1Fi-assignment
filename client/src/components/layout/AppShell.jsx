import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { useCart } from '../../hooks/useCart';

export function AppShell({ children }) {
  const { items, cartNotice, dismissCartNotice } = useCart();
  const [loginOpen, setLoginOpen] = useState(false);
  const itemCount = items.reduce((total, item) => total + item.quantity, 0);

  useEffect(() => {
    if (!cartNotice) return undefined;
    const timeout = window.setTimeout(dismissCartNotice, 2600);
    return () => window.clearTimeout(timeout);
  }, [cartNotice, dismissCartNotice]);

  return <div className="min-h-screen bg-slate-50 pb-24 text-slate-900"><header className="border-b border-slate-200 bg-white/95 backdrop-blur"><nav className="mx-auto flex max-w-7xl items-center justify-between gap-4 px-4 py-4 sm:px-6 lg:px-8"><Link className="flex items-center gap-2 text-xl font-bold tracking-tight text-slate-950" to="/"><span className="grid h-8 w-8 place-items-center rounded-lg bg-indigo-600 text-sm text-white">1</span>Fi</Link><div className="hidden items-center gap-1 md:flex"><Link className="rounded-md px-3 py-2 text-sm font-medium text-slate-600 transition hover:bg-slate-100 hover:text-slate-950" to="/">Home</Link><Link className="rounded-md px-3 py-2 text-sm font-medium text-slate-600 transition hover:bg-slate-100 hover:text-slate-950" to="/">Catalog</Link><a className="rounded-md px-3 py-2 text-sm font-medium text-slate-600 transition hover:bg-slate-100 hover:text-slate-950" href="/#how-it-works">How it works</a><a className="rounded-md px-3 py-2 text-sm font-medium text-slate-600 transition hover:bg-slate-100 hover:text-slate-950" href="/#reviews">Reviews</a><a className="rounded-md px-3 py-2 text-sm font-medium text-slate-600 transition hover:bg-slate-100 hover:text-slate-950" href="/#faqs">FAQs</a></div><button className="rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm font-semibold text-slate-800 transition hover:border-indigo-300 hover:bg-indigo-50" onClick={() => setLoginOpen(true)} type="button">Login</button></nav></header><main className="mx-auto max-w-7xl px-4 py-8 sm:px-6 sm:py-12 lg:px-8">{children}</main><Link aria-label={`Open cart${itemCount ? ` (${itemCount} items)` : ''}`} className={`fixed bottom-5 right-5 z-30 inline-flex h-14 min-w-14 items-center justify-center gap-2 rounded-full bg-slate-950 px-4 text-white shadow-lg shadow-slate-900/25 transition hover:-translate-y-0.5 hover:bg-indigo-700 ${cartNotice ? 'scale-110' : ''}`} to="/cart"><svg aria-hidden="true" className="h-5 w-5" fill="none" stroke="currentColor" strokeWidth="2" viewBox="0 0 24 24"><path d="M3 3h2l2.2 10.3a2 2 0 0 0 2 1.6h7.8a2 2 0 0 0 1.9-1.4L20 7H6" /><circle cx="9" cy="20" r="1" /><circle cx="18" cy="20" r="1" /></svg>{itemCount > 0 && <span className="grid h-5 min-w-5 place-items-center rounded-full bg-indigo-500 px-1 text-xs font-bold">{itemCount}</span>}</Link>{cartNotice && <div aria-live="polite" className="fixed bottom-22 right-5 z-40 rounded-lg bg-emerald-700 px-4 py-3 text-sm font-semibold text-white shadow-lg">{cartNotice}</div>}{loginOpen && <div aria-modal="true" className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4" role="dialog"><div className="w-full max-w-sm rounded-2xl bg-white p-6 shadow-xl"><div className="flex items-start justify-between gap-4"><div><p className="text-sm font-semibold text-indigo-600">1Fi demo</p><h2 className="mt-1 text-xl font-bold text-slate-950">Login preview</h2></div><button aria-label="Close login preview" className="text-slate-500 hover:text-slate-950" onClick={() => setLoginOpen(false)} type="button">×</button></div><p className="mt-3 text-sm leading-6 text-slate-600">Authentication is not enabled in this demo yet. You can continue browsing and complete the available checkout flow.</p><button className="mt-5 w-full rounded-lg bg-indigo-600 px-4 py-2.5 font-semibold text-white hover:bg-indigo-700" onClick={() => setLoginOpen(false)} type="button">Continue browsing</button></div></div>}</div>;
}
