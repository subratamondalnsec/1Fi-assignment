import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { getOrder } from '../services/orderApi';
import { formatCurrency } from '../utils/productHelpers';
import { getRememberedOrderIds } from '../utils/orderHistory';

export function OrdersPage() { const [state, setState] = useState({ status: 'loading', orders: [] }); useEffect(() => { Promise.all(getRememberedOrderIds().map(getOrder)).then((orders) => setState({ status: 'ready', orders: orders.sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt)) })).catch(() => setState({ status: 'ready', orders: [] })); }, []); if (state.status === 'loading') return <p className="py-10 text-center text-slate-600">Loading your demo purchases…</p>; if (!state.orders.length) return <section className="rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center"><h1 className="text-2xl font-bold">No purchases yet</h1><p className="mt-2 text-slate-600">Completed demo purchases will appear here on this browser.</p><Link className="mt-5 inline-flex rounded-lg bg-indigo-600 px-4 py-2.5 font-semibold text-white" to="/#catalog">Explore smartphones</Link></section>; return <section className="space-y-6"><div><p className="text-sm font-bold uppercase tracking-wider text-indigo-600">Demo order history</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Your purchases</h1></div><div className="space-y-4">{state.orders.map((order) => <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm" key={order.id}><div className="flex flex-wrap justify-between gap-3"><div><p className="font-bold text-emerald-700">Order confirmed</p><p className="text-sm text-slate-500">{order.orderNumber}</p></div><p className="text-sm font-semibold text-slate-600">{new Date(order.createdAt).toLocaleDateString('en-IN')}</p></div>{order.items.map((item) => <div className="mt-5 flex gap-4 border-t border-slate-100 pt-5" key={`${item.productId}-${item.variantId}`}><img alt={item.productName} className="h-20 w-20 rounded-xl object-contain bg-slate-50" src={item.imageUrl} /><div className="min-w-0 flex-1"><h2 className="font-bold text-slate-950">{item.productName}</h2><p className="text-sm text-slate-600">{item.storage} · {item.color}</p><p className="mt-2 text-sm text-slate-700">{item.emiTenure} months · {formatCurrency(item.emiMonthlyAmount)}/month</p><p className="text-sm text-slate-600">First payment {formatCurrency(item.firstPaymentAmount)} · Next due {new Date(item.nextDueDate).toLocaleDateString('en-IN', { day: 'numeric', month: 'short' })}</p></div></div>)}<Link className="mt-5 inline-flex font-semibold text-indigo-600 hover:text-indigo-700" to={`/orders/${order.id}/repayment`}>View repayment schedule →</Link></article>)}</div></section>; }
